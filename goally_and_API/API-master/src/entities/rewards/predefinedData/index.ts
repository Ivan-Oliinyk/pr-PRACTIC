import { rewardKarate } from './karate.reward';
import { rewardScreenTime } from './screen-time.reward';
import { rewardSnack } from './snack.reward';

export const predefinedRewards = [rewardKarate, rewardScreenTime, rewardSnack];
